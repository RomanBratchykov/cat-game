// ─────────────────────────────────────────────────────────────────
// System.js
//
// System — ТІЛЬКИ ЛОГІКА. Жодних даних крім посилання на world.
// Кожна система відповідає за одну конкретну відповідальність:
//   PhysicsSystem  — рухає об'єкти
//   RenderSystem   — малює об'єкти
//   CollisionSystem — перевіряє зіткнення
//
// Система отримує доступ до world через this.world (встановлює World
// при реєстрації). Через world.query() вона знаходить всі entities
// з потрібними компонентами і обробляє їх.
//
// Чому не методи на entities:
//   Якщо логіка на entities — entity.update() — тоді кожен тип
//   об'єкта треба окремо оновлювати. Система обробляє ВСІ об'єкти
//   з потрібними компонентами однаково. М'яч і ворог рухаються
//   за однією PhysicsSystem — просто мають однаковий PhysicsComponent.
// ─────────────────────────────────────────────────────────────────

export class System {
  constructor() {
    this.world = null; // встановлює World при world.addSystem(this)
  }

  // Викликається кожен кадр. delta — час з минулого кадру (ms)
  // Перевизначай в підкласах.
  // update(delta) {}
  update() {}

  // Викликається один раз після додавання в world.
  // Корисно для одноразової ініціалізації (наприклад підписка на події)
  init() {}

  // Викликається при знищенні world.
  destroy() {}
}
